# pi-web

`pi-web` exposes standalone web tools for pi.

It currently provides:

- `web_search`
- `fetch_web_content`

Use `web_search` for current information, documentation lookup, and fact
checking.

Use `fetch_web_content` when you already have a specific URL and want readable
page content.
