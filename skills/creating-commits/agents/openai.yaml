interface:
  display_name: "Creating Commits"
  short_description: "Create a commit using Chad's commit template."
  default_prompt: "Use $creating-commits to create a commit from the current changes."
